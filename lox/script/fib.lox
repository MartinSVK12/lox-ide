fun fib(n: number) {
  if (n < 2) return n;
  return fib(n - 1) + fib(n - 2);
}

print("running fib test...");

var before: number = clock();
print(fib(40));
var after: number = clock();
print(after - before);